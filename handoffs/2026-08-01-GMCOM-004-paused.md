# Handoff — GMCOM-004: Connect live Skrybix intake to GM Commerce (PAUSED — not started)

## Task

- Issue: GMCOM-004 (https://github.com/HydraCoreSystems/gm-commerce-hq/issues/4)
- Objective: Integrate the verified Skrybix commerce-selection endpoint
  with GM Commerce so selected plant SKUs enter the workflow without
  manual entry.
- Repository: `HydraCoreSystems/gm-commerce`
- Branch: `main` (unchanged — no new commits this session)

## Work Completed

None. Per this issue's own instructions, I read `DECISIONS.md`,
`STATUS.md`, and the completion handoffs before writing any code, and
found the issue's stated prerequisites were not actually met — see below.
No implementation was started.

## Verification

Before starting, I verified the two stated prerequisites directly rather
than trust the issue text:

- **`gm-commerce` push status:** still unpushed. `git remote -v` in the
  local repo returns nothing; still one local commit
  ("Initial commit... GMCOM-002"). Unchanged since the GMCOM-002 handoff.
- **GMCOM-003 ("verified Skrybix integration"):** does not exist.
  - `git log --all -- app/api/sku-registry` in `skrybix-webapp` returns no
    history at all — the `sku-registry` route (the endpoint GM Commerce's
    reader was built against in GMCOM-002) has never been committed to
    git in that repo, in any commit. It exists only as uncommitted local
    changes on this machine (`app/api/sku-registry/` untracked;
    `middleware.ts` and `.env.example` modified but not committed) — so
    it has never been deployed and is not actually reachable at
    `https://skrybix-webapp.vercel.app/api/sku-registry`.
  - A repo-wide search for any "select for commerce" mechanism
    (`selected_for_commerce`, related schema columns, UI, or routes) in
    `skrybix-webapp` found nothing. There is no way, today, for a human to
    mark a Skrybix plant/cutting as selected for commerce — the second
    step of GMCOM-004's own required workflow.

I flagged this to Phil rather than proceeding. His direction, relayed
back: GMCOM-004 stays paused; do not build an interim workaround; Copilot
is finishing the real Skrybix-side selection mechanism under a reopened
GMCOM-003 (Copilot separately completed a related non-plant handoff under
GMCOM-005). Resume GMCOM-004 once GMCOM-003 is complete and verified,
using its final handoff contract.

## Decisions Made

- No architectural decisions made this session. The one durable fact
  worth recording: GMCOM-003 was reopened (it was never actually
  completed despite GMCOM-004 assuming otherwise) and Copilot is now
  finishing it. Recommend the project manager reconcile the GitHub Issue
  states (GMCOM-003 reopened, GMCOM-004 blocked) so this isn't
  rediscovered the same way next time.

## Remaining Work

Everything in GMCOM-004's acceptance criteria — none of it has been
started. Blocked entirely on GMCOM-003 landing for real:

- A real "select for commerce" mechanism in Skrybix (schema + UI).
- The `sku-registry` (or successor) endpoint actually committed and
  deployed, not just sitting locally uncommitted.
- A defined handoff contract (what shape of data GM Commerce should
  consume, and how "already imported" is distinguished from "newly
  selected") — GMCOM-004 says to use "the final Skrybix handoff contract,"
  implying this may differ from the read-everything `sku-registry` shape
  GM Commerce's `lib/sources/skrybix.ts` currently expects. That file will
  likely need real changes once the contract is known, not just a
  connected env var.

## Blockers or Risks

- **GMCOM-003 is not done** — this is the hard blocker on all of
  GMCOM-004.
- `gm-commerce` still isn't pushed to GitHub (Phil's action item, no
  push credentials available to Claude in this environment).
- Worth naming for the project manager: the same gap (an issue assuming a
  prerequisite was done when it wasn't) already caused one paused turn
  here. Confirming an issue's stated prerequisites against the actual repo
  state before assignment, not just before implementation, would avoid
  this earlier in the loop.

## Recommended Next Action

Wait for Copilot to complete GMCOM-003 (real selection mechanism in
Skrybix, `sku-registry`-or-successor endpoint actually committed and
deployed) and for its final handoff to define the integration contract.
Once that lands, resume GMCOM-004 against that contract — do not resume
against the current unselective `sku-registry` shape, since that doesn't
satisfy GMCOM-004's "human selects it in Skrybix for commerce" requirement
regardless of whether the endpoint is deployed.
